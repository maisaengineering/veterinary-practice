%h4 Below are the List of Appointments Registered for customers
- unless @appointments.blank?
  %table{class: "table table-striped table-bordered table-condensed"}
    %thead
      %tr
        %th #
        %th Pet's Name
        %th Date of next Appointment
        %th Reason for visit
    %tbody
      - count = 1
      - @appointments.each do |appointment|
        %tr
          %td= count
          %td= appointment.pet
          %td= appointment.date_of_visit
          %td= appointment.reason_for_visit.truncate(100, omission: '...')
        - count += 1
